<%= form_with(model: law_category, local: true) do |form| %>
  <% if law_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(law_category.errors.count, "error") %> prohibited this law_category from being saved:</h2>

      <ul>
      <% law_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
