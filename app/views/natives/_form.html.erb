<%= form_with(model: native, local: true) do |form| %>
  <% if native.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(native.errors.count, "error") %> prohibited this native from being saved:</h2>

      <ul>
      <% native.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :locations_id %>
    <%= form.text_field :locations_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
