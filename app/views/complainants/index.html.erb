<p id="notice"><%= notice %></p>

<h1>Complainants</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Last name</th>
      <th>Birthdate</th>
      <th>Address</th>
      <th>Nationality</th>
      <th>Gender</th>
      <th>Disability</th>
      <th>Migrant</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @complainants.each do |complainant| %>
      <tr>
        <td><%= complainant.name %></td>
        <td><%= complainant.last_name %></td>
        <td><%= complainant.birthdate %></td>
        <td><%= complainant.address %></td>
        <td><%= complainant.nationality %></td>
        <td><%= complainant.gender %></td>
        <td><%= complainant.disability %></td>
        <td><%= complainant.migrant %></td>
        <td><%= link_to 'Show', complainant %></td>
        <td><%= link_to 'Edit', edit_complainant_path(complainant) %></td>
        <td><%= link_to 'Destroy', complainant, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Complainant', new_complainant_path %>
