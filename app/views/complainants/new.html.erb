<div class="row">
  <div class="col-md-6">
    <div class="card">
      <div class="card-header card-header-primary">
        <h4 class="card-title">
          Añade un nuevo Denunciante
        </h4>
        <p class="card-category">
          Añade un nuevo denunciante al sistema
        </p>
      </div>
      <%= render 'form', complainant: @complainant%>
    </div>
  </div>
</div>

<%= link_to 'Back', complainants_path, class: "btn btn-outline-secondary btn-sm" %>