<div class="card-body">
  <%= form_with(model: instittution, local: true) do |form| %>
    <% if instittution.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(instittution.errors.count, "error") %> prohibited this instittution from being saved:</h2>

        <ul>
        <% instittution.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="row">
      <div class="col-md-12">
        <div class="form-group">
          <%= form.label :name, class:"bmd-label-floating" %>
          <%= form.text_field :name, class:"form-control" %>
        </div>
      </div>
    </div>

    <%= form.submit "Guardar", class:"btn btn-primary pull-rigth" %>
    <div class="clearfix"></div>
  <% end %>
</div>
