<div class="card-body">
  <%= form_with(model: court, local: true) do |form| %>
    <% if court.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(court.errors.count, "error") %> prohibited this court from being saved:</h2>

        <ul>
        <% court.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="row">
      <div class="col-md-12">
        <div class="form-group">
          <%= form.label :name, class:"bmd-label-floating" %>
          <%= form.text_field :name, class:"form-control" %>
        </div>
      </div>
      <%# <div class="col-md-12">
        <div class="form-group">
          <%= form.label "Provincia", class:"bmd-label-floating" %>
          <%#<%= form.select :city_name, options_from_collection_for_select(City.all, "id", "city_name", 104), class:"form-control" %>
       <%# </div>
      </div> %>

      <div class="row">
        <div class="col-md-6">
          <div class="form-group">
            <%= form.label "Provincia", class:"bmd-label-floating" %>
            <%= form.collection_select :city_id, City.argentina, :id, :city_name, include_blank: true %>
          </div>
        </div>
        <div class="col-md-6">
          <div class="form-group">
            <%= form.label "Jurisdiccion", class: "bmd-label-floating" %>
            <%= form.grouped_collection_select :jurisdiction_id, City.argentina, :jurisdiction, :city_name, :id, :name, include_blank: true %>
          </div>
        </div>
      </div>
    </div>

    <%= form.submit "Guardar", class:"btn btn-primary pull-rigth" %>
    <div class="clearfix"></div>
  <% end %>
</div>
